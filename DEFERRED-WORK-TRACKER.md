> **Catatan (19 Sep 2026):** File laporan/audit/decision lama (GO-LIVE-BUSINESS-READINESS.md,
> laporan-*, dll) yang sebelumnya berceceran di root repo kini ada di `DOCS/ARCHIVE`,
> `DOCS/AUDIT`, `DOCS/DECISIONS`, `DOCS/`. Lihat commit "docs: reorganize scattered
> .md files into DOCS/ structure (VPS evacuation cleanup)".

# DEFERRED WORK TRACKER

> **Tujuan file ini:** satu sumber kebenaran untuk SEMUA keputusan defer/
> penundaan scope yang disengaja di seluruh project — beda dari
> `BUG-BELUM-DIBERESKAN.md` (yang mencampur bug + defer + resolved).
> File ini KHUSUS untuk hal yang SENGAJA ditunda dengan alasan eksplisit,
> supaya tidak ada lagi kasus seperti PV-P3 (magic-paste variant
> extraction) — kontrak induknya ditandai "LOCKED/mostly done" padahal
> satu sub-scope di dalamnya belum mulai sama sekali dan tidak
> ter-track di mana pun.
>
> **Aturan wajib:** setiap TASK yang sengaja men-defer sebagian scope
> HARUS menambah baris di sini pada sesi yang sama — bukan disebut sekali
> di laporan lalu hilang. Setiap baris WAJIB punya kolom "Trigger" — kondisi
> konkret kapan item ini harus ditagih kembali, bukan "nanti" yang kabur.
>
> **Update terakhir:** 6 Sep 2026 (post-INT-L10 commit `471f50a` — smoke test complete).

---

## 🔴 Prioritas tinggi — blocking sebelum go-live atau berdampak signifikan

| # | Item | Dari task/kontrak | Kenapa di-defer | Trigger untuk ditagih |
|---|------|-------------------|------------------|------------------------|
| 2 | Billing / pricing model — TIDAK ADA sama sekali di codebase | `GO-LIVE-BUSINESS-READINESS.md` #1 | Keputusan bisnis, bukan teknis — belum diputuskan free/paid/trial/per-message | Sebelum onboarding merchant riil pertama yang bukan test/canary |
| 3 | Terms of Service / Privacy Policy — TIDAK ADA, padahal platform simpan PII terenkripsi (UU PDP applicable) | `GO-LIVE-BUSINESS-READINESS.md` #3 | Legal requirement, tapi butuh keputusan/draft di luar kode | Sebelum onboarding merchant riil pertama (legal blocker, bukan opsional) |
| 33 | Kapasitas multi-provider: chat_primary aktif NVIDIA (openai/gpt-oss-20b), GroqNew (openai/gpt-oss-20b); chat_fallback aktif SambaNova (MiniMax-M2.7), B.ai (hy3), Gemini (gemini-3.5-flash-lite), Kilo (kilo-auto/free). Mistral (chat_primary) inactive, LLM7.io (chat_primary) kena RATE_LIMIT quota exceeded, Laguna (chat_fallback) inactive. N-provider rotation aktif (resolver dinamis U5 `cced1ce`), jauh melampaui konfigurasi 2-provider (Mistral+SambaNova) yang men-trigger item ini. Kapasitas kuota provider aktif perlu dipantau sebelum live production — sebelumnya 429 berulang karena limit combined Mistral+SambaNova, sekarang ada 5 fallback aktif. **Catatan:** 3rd-tier fallback "Internal LLM" (Gemini via webai-to-api browser-session proxy) sudah DIBERHENTIKAN — lihat bagian ✅ RESOLVED "Internal-LLM". Diverifikasi ulang: 19 Sep 2026, live query psql — lihat commit ini. | P2-UNIT5 testing, tracker item lama #33 | Claude/robot — tidak perlu setup tambahan. Pandjie — monitor status provider aktif via dashboard AI Providers sebelum live session penting | **Upgrade trigger:** SEBELUM onboarding merchant asli pertama — verifikasi kuota NVIDIA/GroqNew (chat_primary) dan SambaNova/B.ai/Gemini/Kilo (chat_fallback) cukup untuk trafik production. Jangan aktifkan v2 penuh ke customer kalau masih ada provider yang kena RATE_LIMIT/INSUFFICIENT_QUOTA. |
| 40 | **hapus reasoning.ts (v2-lama) permanen** — `src/services/chat/reasoning.ts` (legacy `InterpreterResultV2` / acts-based tier chain) **tidak lagi di-import** oleh active path `conversation.service.ts` (§4–§6), tapi masih ada di repo sebagai catatan. Dipertahankan demi rollback cepat bila observasi menemukan regresi. Perlu dihapus permanen setelah periode observasi tanpa insiden. **Tambahan: gatekeeper dead-code removal — `extractIntent` methods pada `AIProviderManager` (`manager.ts:122`) dan `LLMGateway` (`llm-gateway.ts:479`), export `container.ts:32-33`, implementasi `GroqAdapter.extractIntent` (`groq.adapter.ts:329`), dan hardcoded `groqAdapter`/`geminiAdapter` singleton defaults di kedua manager (`manager.ts:48-50`, `llm-gateway.ts:98-102`) juga confirmed unreachable dari active `rewriteMode==='active'` branch (sesi 18 Sep 2026 — full call-chain trace: `conversation.service.ts` → `callV2Engine` → `engine-call.ts` tidak pernah memanggil `extractIntent`; `message-processor.service.ts` juga tidak). Tidak di-remove terpisah agar tidak cleanup yang tidak terkoordinasi — saat reasoning.ts dihapus, audit bersamaan apakah gatekeeper machinery juga aman untuk dihapus, karena kedua-duanya confirmed dead oleh bukti yang sama. | RAILS §6 (14 Sep 2026) — v2-rewrite ACTIVE untuk semua toko (`chatEngine.v2RewriteMode='active'`) | Butuh observasi beberapa hari tanpa masalah besar sebelum menghapus fallback/rollback path; satu-satunya jalur aktif sudah v2-rewrite | **SELESAI 19 Sep 2026** — reasoning.ts + gatekeeper machinery dihapus permanen. reasoning.ts unreachable karena v2RewriteMode='active' global; baris 737/1367 ada di dalam blok legacy yang tidak dieksekusi. Stubbed dengan inert placeholder di conversation.service.ts. DWTR #NEW mencatat blok legacy v1/v2 (lines 662-1355) sebagai item terpisah untuk evaluasi cleanup masa depan. Commit: `22f309c` |

## 🟡 Prioritas menengah — perlu ditagih tapi tidak blocking

| # | Item | Dari task/kontrak | Kenapa di-defer | Trigger untuk ditagih |
|---|------|-------------------|------------------|------------------------|
| 5 | Multi-key rotation dalam SATU role provider (`chat_primary`/`chat_fallback`) — resolver cuma ambil provider top-1 per role, tidak rotasi antar-key kalau limit | LLM-PROVIDER-ABSTRACTION Unit 3b/4 | Owner memilih pindah ke OpenRouter (rotasi provider bawaan) sebagai solusi, bukan dibangun sendiri | Kalau OpenRouter free-tier (50 req/hari tanpa topup) ternyata tidak cukup dan builtin rotation dibutuhkan lagi |
| NEW | Blok legacy v1/v2 di `conversation.service.ts` lines 662-1355 (termasuk `getStoreEngine` branching + shadow hook v3.2) — seluruh blok ini unreachable saat `chatEngine.v2RewriteMode='active'` (global default). Baris 662+ hanya dicapai jika flag != 'active'; shadow hook (line 1357) tergantung pada `shouldRunShadow()` yang default false. Tidak ada traffic yang menempuh jalur ini di config saat ini. reasoning.ts (yang jadi bagian dari jalur ini) sudah dihapus permanen 19 Sep 2026. | Audit 19 Sep 2026 — rekonsiliasi kontradiksi reasoning.ts | Owner konfirmasi v1 dihapus total (kontrak §0.1), tidak ada kebutuhan rollback cepat | **Trigger:** kalau ada kebutuhan bersih-bersih lebih lanjut, atau kalau `v2RewriteMode` dikonfirmasi tidak akan pernah per-store lagi (selalu global) — evaluasi hapus seluruh blok legacy branch sebagai TASK terpisah. Prioritas menengah, bukan tinggi — tidak ada traffic yang terdampak. |
| 7 | Track B — Wizard UI (P2) + LLM generation service (P3) belum mulai, baru P1 (schema) selesai | `PROJECT-CONTRACT-ONBOARDING-WIZARD.md` §6 | Sengaja dipisah per-unit dari P1; Track A (LLM provider abstraction) diprioritaskan duluan supaya wizard generation langsung pakai sistem provider dinamis | Setelah Track A settle (flag ON atau OpenRouter terpasang) — TIDAK ada alasan lain untuk menunda lebih lama |
| 8 | Chat gatekeeper (`extractIntent`) tetap pinned ke `groqAdapter` singleton, TIDAK ikut resolver dinamis — `chat_gatekeeper` role di dashboard kosmetik | LLM-PROVIDER-ABSTRACTION Unit 5 Part 1 (Option B) | `extractIntent` bukan bagian interface `AIProvider`; Option A (bikin optional method) berisiko silent-degradation | Kalau ada kebutuhan konkret ganti gatekeeper dari Groq (bukan sekadar "biar konsisten") |
| 11 | IX-D — Guard `hasVariants && !variantId` duplikat di 2 tempat (`cart-authority.ts` + `action-registry.ts`), belum di-DRY | `BUG-BELUM-DIBERESKAN.md` §IX-D | Konsisten, tidak ada kontradiksi — refactor kosmetik | Siklus maintenance CartAuthority berikutnya |
| 12 | Merchant onboarding wizard (guided setup UX untuk connect WA/tambah produk) — beda dari Track B wizard di atas (yang itu TOS/SOP/FAQ) | `GO-LIVE-BUSINESS-READINESS.md` #2 | Registrasi teknis sudah jalan, tapi UX guided untuk merchant non-teknis minimal | Kalau ada keluhan merchant riil kesulitan onboarding sendiri |
| 17 | Cost accuracy per-provider (`TokenUsageLog.costUsd` pakai default $0.05/$0.15 generik, bukan tarif asli Mistral/SambaNova/provider baru lainnya) | TOKEN-USAGE-UNIT1-PERSISTENCE | Owner eksplisit bilang tidak perlu sekarang — requests/tokens sudah akurat, cost cuma perkiraan kasar | Kalau owner butuh laporan biaya aktual yang akurat (mis. untuk billing decision di GO-LIVE-BUSINESS-READINESS.md item #1) |
| 19 | `batch-magic-paste.e2e.test.ts` tests #7/#8 fail — send no-weight items ("Rendang 50000"/"Sate 20000") expecting creation, but the weight gate correctly blocks them. Confirmed PRE-EXISTING via `git stash` (not caused by today's PV-P3/weight-gate-visibility fix) | DEBUG-MAGIC-PASTE-VARIANT-NOT-SHOWING-AND-EMPTY-LIST, 2 Sep 2026 | Test expectations are stale — written before/without accounting for the weight gate's blocking behavior; not a product bug | Kapan saja — update the 2 test cases to either include weight in the input text, or explicitly assert needsWeightInput:true instead of expecting creation |
| 20 | Dashboard frontend anti-pattern: beberapa halaman mengecek `res.data.success` secara kasar tanpa membaca field detail lain (mis. `needsWeightInput`, `variants`) — sudah ketemu 3x hari ini (AI Providers error display, magic-paste false-success). Belum diaudit menyeluruh apakah ada halaman lain dengan pola sama | Ditemukan berulang selama sesi 2 Sep 2026 | Perbaikan spesifik sudah dilakukan tiap kali ditemukan; audit MENYELURUH ke semua halaman dashboard belum dilakukan | Kalau ketemu lagi kasus serupa di halaman lain, atau saat ada waktu luang buat audit preventif |
| 21 | Stale `finally` cleanup RESTRICT pattern di `golden-dataset.test.ts` — `Case G2-D.8` (L1097) `finally` (L1185-1190) delete `orderItem`→`order`→`conversation` **tanpa** `conversation_history`/`conversation_context` dulu. FK RESTRICT (`conversation_history_conversationId_fkey` → `conversations.id`) bikin `prisma.conversation.delete` (L1189) **gagal** — error ini dikonfirmasi empirical dengan menjalankan G2-D.8 terisolasi (`--test-name-pattern='Case G2-D.8:'`): tetap **PASS 1/1**, tapi `finally` mencetak `prisma:error … code "23001" … "update or delete on table \"conversations\" violates RESTRICT setting of foreign key constraint \"conversation_history_conversationId_fkey\" on table \"conversation_history\"… Key (id)=(conv-g2d8) is referenced from table \"conversation_history\""`. `.catch(() => {})` (L1189) menekan exception, tapi row `conversation_history` conv-g2d8 **orphan sampai `beforeEach` berikutnya** (L312 `conversationHistory.deleteMany` by `storeId` — bukan by `conversationId`, jadi bocor lintas test selama satu siklus). Perbaikan baru: `P3b-INVARIANT` (a)/(b) `finally` (L1359-1370) pakai urutan benar (history→context→orderItem→order→conversation) → tidak error. G2-D.8 + ~9 test lain di file ini masih pakai pola lama (sama: orderItem→order→conversation tanpa history/context). | Sesi 16 Sep 2026 — Part 3 (extend P3b-INVARIANT + dokumentasi) | Owner: **JANGAN fix** sekarang (ini test-hygiene, bukan bug produk; production code tidak tersentuh). **Trigger konkret:** bila `npm run test:golden` mencetak `prisma:error … violates RESTRICT … conversation_history_conversationId_fkey` pada G2-D.8 (atau test lain) ATAU test flaky karena row `conversation_history`/`order` bocor antar-test — saat itu, ganti urutan `finally` semua test pola lama (G2-D.8 L1185-1190 dst.) jadi pola `P3b-INVARIANT` (L1359-1370): `conversationHistory.deleteMany` → `conversationContext.deleteMany` → `orderItem` → `order` → `conversation`. |
| 22 | Product delete tidak cascade-delete ProductVariant rows — varian menjadi orphaned setelah product di-soft-delete. Sudah diperbaiki dengan hard-delete varian dalam transaction yang sama | VARIANT-LIFECYCLE-FULL-AUDIT-AND-FIX, 3 Sep 2026 | Bug ditemukan saat audit, perbaikan sudah diterapkan dan ditest | Tidak ada — sudah diperbaiki |
| 37 | 3 test file (`circuit-breaker-per-message`, `v2-schema-enum-expansion`, `v2-update-shipping-address`) gagal karena `@jest/globals` module incompatibility — error: `SyntaxError: The requested module '@jest/globals' does not provide an export named 'beforeEach'`. Dikonfirmasi PRE-EXISTING via `git stash` (sama-sama gagal di kode lama, sebelum perubahan EXPAND-V2-REWRITE-TO-ALL-STORES). Tidak terkait dengan perubahan v2-rewrite, provider testing, atau schema expansion. | NOTE-JEST-GLOBALS-PREEXISTING-FAILURE, post-EXPAND-V2-REWRITE-TO-ALL-STORES | 3 file test ini import `@jest/globals` (seperti `beforeEach`, `beforeAll`, `afterEach`) di proyek TypeScript yang menggunakan Node.js native `node:test` runner, bukan Jest. `@jest/globals` tidak memiliki ekspor NAMED exports yang sama di ESM context — hanya bekerja di Jest runtime. | Trigger: perbaiki kalau test ini mulai gagal di CI (jika CI pipeline berubah ke Jest-compatible), atau kapan pun ada waktu luang — ganti import `@jest/globals` → `@node/test` hooks (`before`, `after`, `beforeEach`/`afterEach` via `node:test`) |
| 38 | Model override anomali di `GroqNew` — `groq.adapter.ts:116` unconditionally overwrites `this._model` ke `defaults.fallbackModel` (`openai/gpt-oss-120b`) di setiap `generate()` call, terlepas dari nilai `ai_provider_configs.model` untuk row `GroqNew` (`openai/gpt-oss-20b`). Artinya model yang terdisplay di admin dashboard untuk GroqNew TIDAK cocok dengan model yang benar-benar dipanggil di runtime. | Audit 18 Sep 2026 — Part 2 | `skipParams` semua `chat_primary` provider kosong (tidak ada kebutuhan analog Internal LLM INT-L10); 400 `json_validate_failed` adalah known guard gpt-oss max_tokens floor (1024) yang sudah ter-handle di `groq.adapter.ts:124-126`. Namun model mismatch antara DB dan runtime adalah anomali yang belum diinvestigasi — bukan bug produksi (hasil LLM tetap valid), tapi menyesatkan untuk debugging/observability. | Kalau ada kebutuhan verifikasi model yang benar-benar dipakai per provider, atau kalau owner mau switch ke model `gpt-oss-20b` beneran jalan — cek override ini dulu sebelum mengubah `ai_provider_configs.model` |

---
 — seharusnya punya kategori sendiri `modify_order`.** UPDATE_SHIPPING_ADDRESS ditambahkan sebagai canonical action_type di schema.ts (requires_validation=true), tapi karena `CHAT-ENGINE-V2-DESIGN-P1.md` mendefinisikan `modify_cart` khusus untuk *cart-item CRUD* (ubah/kurangi/hapus item di keranjang), bukan mutasi level-order, mapping ini semanticalnya salah. UPDATE_SHIPPING_ADDRESS memodifikasi **alamat pengiriman pada pesanan yang sudah ada** — konsep berbeda dari cart item manipulation. Perlu kategori baru `modify_order` untuk mutasi level-order (alamat, metode kirim, metode bayar SETELAH checkout) begitu ada implementasi eksekusi nyata untuk aksi ini. | ADD-UPDATE-SHIPPING-ADDRESS-ACTION-TYPE | Pragmatic-first: menggunakan modify_cart agar tidak breaking, memungkinkan validation + translation layer berjalan. Kategori modify_order butuh design sign-off + eksekusi backend (order update API) yang belum ada | **Trigger:** saat fitur "ubah alamat pasca-checkout" benar-beny mulai dibangun (bukan hanya skema/klasifikasi) — jangan bikin modify_order sebelum ada kebutuhan eksekusi nyata. Saat itu, evaluasi ulang apakah modify_order perlu intent baru di kontrak V2 atau cukup reason/kategori baru di conversation.service.ts. |

## 🟢 Prioritas rendah — hygiene/cosmetic, aman ditunda lama

| # | Item | Dari task/kontrak | Kenapa di-defer | Trigger untuk ditagih |
|---|------|-------------------|------------------|------------------------|
| 13 | III-6 — Golden dataset invarian I8-I15 masih test unit parsial, bukan 50-case permanen | `BUG-BELUM-DIBERESKAN.md` §III-6 | Regression coverage belum lengkap, tidak blocking | Kapan saja, prioritas rendah |
| 14 | II-5 — Test DB shared isolation lemah (row lintas file test) | `BUG-BELUM-DIBERESKAN.md` §II-5 | Sudah di-audit "0 assertion rawan", downgrade ke hygiene debt | Kalau ada file test baru — re-audit saat itu |
| 19 | Magic-paste kadang gabungkan huruf varian pertama ke nama produk (mis. "Baju polos S" bukan "Baju polos") pada input 1-baris 2-varian — keterbatasan LLM, bukan bug kode, dimitigasi oleh preview-sebelum-simpan | DEBUG-CATALOG-EMPTY-AFTER-CREATE, 2 Sep 2026 | LLM variance pada kalimat ambiguous, preview UX sudah jadi jaring pengaman | Kalau owner sering ketemu ini dan preview-edit terasa merepotkan |
| 23 | Redis-backed provider cooldown (`provider-cooldown.ts` pakai `Map` in-memory, hanya valid untuk single pm2 instance — cooldown state hilang saat restart, tidak ter-share antar instance) — dibutuhkan jika scaling ke multi-instance pm2 atau butuh cooldown persisten | N-PROVIDER-ROTATION-UNIT-N1, 3 Sep 2026 | Owner konfirmasi target realistis: 3-5 provider pada single-instance; in-memory cooldown sudah sufficient. Redis migration butuh infra setup tambahan | Kalau owner pindah ke multi-instance pm2, atau butuh cooldown state persist across restarts |
| 32 | Root cause asli "Decryption failed: Store.phoneNumber" pada store-4f4f67bd DITEMUKAN. Key rotation reset (KEY_A ditimpa, data enkripsi KEY_B) (kunci di .env dan DB terkonfirmasi sama, jadi bukan mismatch env). Toko masih active di DB pada 12:29-12:30; dihapus manual setelah test selesai (bukan "sesi sebelumnya"). Traffic @12:30:35 via direct API port 3000, bukan nginx; dist/ lama masih punya SHADOW_STORE_ID='store-4f4f67bd' sampai rebuild Sep 5 commit 552d489 | URGENT diagnosis + cleanup, 5 Sep | Semua toko dummy, tidak ada dampak nyata — cleanup lebih efisien daripada investigasi mendalam. Tapi row corrupt yang sebenarnya (bug di key-rotation.service.ts, atau corruption lain) belum terbukti sudah tertutup | Kalau kejadian serupa muncul lagi di toko manapun (termasuk toko baru) — investigasi ulang lebih dalam, JANGAN asumsikan "pasti sudah aman" hanya karena kunci cocok |
| 34 | Fast-path keyword collision: "kapan pesanan akan sampai?" (delivery-time inquiry) tertangkap oleh tryTotal via substring "pesanan" di TOTAL_TRIGGERS (tier-match.ts:37); tryOrderStatus (fallback.service.ts:112) tidak match karena ORDER_STATUS_KEYWORDS (tier-match.ts:164-176) butuh frasa eksak ("kapan dikirim"/"status pesanan"/"pesanan saya") bukan "pesanan" sendiri. Respons salah: "keranjang belanja masih kosong" (fastpath, llmCallCount=0). CLASS-BUG: substring keyword collision, pola sama dengan bug ga/ready ga sebelumnya. DIBATALKAN fix: v1 sedang dihapus total (kontrak 0.1), tidak ada nilai memperbaiki tier yang akan mati. Dicatat dokumentasi kelas-bug saja. | Investigasi Sep 5 2026, fast-path wrong-answer | v1 akan dihapus total | Tidak ada: non-actionable selama v1 ada, auto-resolved saat v1 dihapus |

---

## Cara pakai file ini

- **Setiap TASK baru yang sengaja skip sebagian scope** → tambah baris di
  tabel yang sesuai prioritas, WAJIB isi kolom Trigger dengan kondisi
  konkret (bukan "nanti"/"someday").
- **Sebelum mulai kontrak/fitur besar baru** → scan file ini dulu, cek
  apakah ada defer lama yang triggernya sudah terpenuhi tapi belum ditagih.
- **Item yang sudah selesai** → pindahkan ke bagian bawah sebagai
  "✅ RESOLVED" dengan tanggal + commit, JANGAN dihapus (audit trail).
- **Push discipline:** jangan biarkan commits menumpuk local-only selama
  berjam-jam. Hari ini 8 commits duduk local-only ~8 jam sebelum di-push
  (akibat task demi task tanpa push intermediate). Rekomendasi: push
  segera setelah acceptance criteria satu unit terpenuhi — jangan tunggu
  akhir sesi. Ini mencegah gap local-vs-origin yang berbahaya kalau
  session terputus (lihat kasus AI-Providers 404 hari ini: code ada di
  local tapi production masih jalan versi lama).

## ✅ RESOLVED

| # | Item | Resolved | Commit(s) |
|---|------|----------|-----------|
| 41 | **Backup encryption key not used in CLI manual backups** — `BACKUP_ENCRYPTION_KEY` configured in `.env` (64-char hex) dan tersimpan di DB `system_settings`, tapi backup dibuat via `npm run backup:create` terenkripsi dengan EMPTY KEY. Root cause: `apps/api/scripts/backup-create.ts` adalah standalone script yang TIDAK memuat `dotenv` dan TIDAK memanggil `reconfigureBackupConfig()` (hanya dipanggil di `index.ts` → `initAdapters()` saat pm2 start). Akibatnya `backupConfig.encryptionKey` tetap `''` dan `crypto.scryptSync('', 'garuda-backup-salt', 32)` menghasilkan key kosong. **Hanya mempengaruhi backup CLI manual** — backup terjadwal via pm2 (`scheduleBackups.ts`) aman karena lewat proses yang sudah panggil `initAdapters()`. Konfirmasi: decrypt backup terjadwal `backup_2026-09-20T19-00-00-129Z_38ffe70c.sql.gz.enc` dengan configured key berhasil tanpa fallback; decrypt backup manual `backup_2026-09-20T17-03-03-637Z_bdc8654b.sql.gz.enc` gagal tanpa fallback. **Fix:** tambah `dotenv.config({ override: false })` + `await reconfigureBackupConfig()` di awal `backup-create.ts`. Verifikasi: backup baru via `npm run backup:create` setelah fix berhasil decrypt dengan configured key saja (701KB, 0.67MB). | 21 Sep 2026 | `fix(deploy): split restore-database.sh...` (`cae8da9`) + `fix(backup): load dotenv + reconfigureBackupConfig in backup-create.ts` |
| 4 | VII-A — Rotate seluruh secret (DATABASE_URL, REDIS_URL, GEMINI_API_KEY, GROQ_API_KEYS, GOWA_BASIC_AUTH_*, CLOUDINARY_*, BACKUP_ENCRYPTION_KEY, WEBHOOK_SECRET, STORAGE_PROVIDER/R2_*, FIELD_ENCRYPTION_KEY, CLOUDFLARE_WORKER_*, PUBLIC_API_KEY). RAJAONGKIR_API_KEY confirmed NOT rotated (never exposed, added post-purge). All keys confirmed changed from exposed pre-purge values, app healthy on new credentials (pm2 online, /api/health 200). | 3 Sep 2026 | Owner-completed outside session; verified 3 Sep 2026 |
| 15 | `GITHUB_PAT`, `WEBHOOK_SECRET` — removed from `.env.example` and confirmed 0 code references. Live `.env` untouched (not relied upon silently). | 3 Sep 2026 | `3 Sep 2026 session` |
| 9 | `adapters.llm.chat` — removed from `container.ts`. Confirmed zero non-test production references (only a comment in `manager-dynamic.test.ts`). Distinct from `adapters.ai.generate` (the active magic-paste path via `product.service.ts:1089`). Unrelated to N-provider rotation (#16), which targets resolver/gateway layer. | 3 Sep 2026 | `3 Sep 2026 session` |
| 10 | Live-refresh `lastTestedAt`/`lastTestResult` di dashboard AI Providers — `handleTestRow` kini update state lokal provider row (lastTestedAt + lastTestResult='ok'/'failed') langsung setelah test selesai, tanpa perlu reload halaman. Draft test flow (`handleTestDraft`) tidak terpengaruh (tidak ada row di table). | 3 Sep 2026 | `3 Sep 2026 session` |
| 18 | `adapters.knowledge`, `adapters.storage`, AND `adapters.llm` stub properties removed from `container.ts` exports (dead code, 0 non-test references). N-provider rotation (#16) is unrelated — it extends resolver/gateway, not the container property. | 3 Sep 2026 | `3 Sep 2026 session` |
| 24 | `products-routes.e2e.test.ts` tests #9/#11/#13 — added `weight: 1` to request bodies (stale expectations, not product bug). All 15 tests now pass. | 3 Sep 2026 | `3 Sep 2026 session` |
| 25 | `ProductFormModal.tsx` — deleted (0 references anywhere, superseded by shared `ProductForm.tsx`). | 3 Sep 2026 | `3 Sep 2026 session` |
| 1 | PV-P3 — Magic-paste variant extraction. Best-effort LLM parsing (variants[] with attributes/price/stock per option, threshold: ≥2 distinct prices or distinct stock+shared price), preview+edit step before save (ConfirmCreateModal), transactional create (atomic Product+ProductVariant write, P2002→clean 409, no partial rows), merchant variantOverrides take precedence over raw LLM output. No hard gate (unlike needsWeightInput) — always produces an editable preview, never blocks creation. | 2 Sep 2026 | `adba501` (Unit 1 — parsing), `7950533` (Unit 2 — transactional create + dashboard preview/edit + e2e a-g, regression 271/37/118/46/8) |
| 26 | Dual-UI magic-paste drift (ConfirmCreateModal vs ProductsPage) — merchant UI kini punya full variant editing (preview + create + post-edit), menyamakan fitur yang sebelumnya hanya ada di admin UI. Root cause bug varian sesi 2-3 Sep 2026. | 3 Sep 2026 | `2800f54` (variant lifecycle audit + merchant variant editing) |
| 27 | Product CRUD dual-UI consolidation — AdminProductsPage, ProductsPage, dan ProductDetailPage kini semuanya pakai shared `ProductForm` component. VariantManagementPanel routing bug lama (double-prefix `/api/admin/variants/stores/...`) ditemukan+diperbaiki. ProductDetailPage variant editing di-dedup ke 1 sumber kebenaran (`VariantManagementPanel` saja, ProductForm modal di page itu diset `showVariantSection=false`). | 3 Sep 2026 | `cd5a8d1` (Unit 1 — shared ProductForm), `7505974` (ProductDetailPage wire), `483bbbc` (routing fix + dedup) |
| 6 | `llm.useDynamicProviders` flag — sekarang ON di production. Dikonfigurasi Mistral (chat_primary) + SambaNova (chat_fallback), diverifikasi via real smoke test: Mistral melayani chat message nyata (token counts 619 in / 85 out cocok dengan interpreter logs). Token usage sekarang di-persist ke DB (TokenUsageLog) + dashboard UI (TokenUsage.tsx) untuk query range fleksibel. | 2 Sep 2026 | Working tree (uncommitted) — flip-flag + TOKEN-USAGE-Unit1 (schema migration + persistence + query endpoint + 9 tests) + TOKEN-USAGE-Unit2 (dashboard UI) + ORPHAN-AUDIT. Base: `bc53d0e` (Unit 4 — flag-gated cutover), `cced1ce` (U5 FINAL) |
| 16 | N-provider rotation dalam 1 role — **IMPLEMENTED** di `llm-gateway.ts` + `manager.ts`. `resolveEffectiveProviders` kini mengembalikan full priority-ordered list per role (bukan hanya `primaryList[0]`/`fallbackList[0]`). `generate()` (keduanya) meng-iterasi penuh per-role provider list: skip provider yang sedang cooldown (`shouldSkipProvider`), coba provider pertama non-cooldown, pada 429/RATE_LIMIT trigger cooldown dan beriksar ke provider BERIKUTNYA dalam role YANG SAMA (bukan langsung fallthrough ke role lain) — baru fallthrough primary→fallback ketika role list YANG SEDANG DIPAKAI benar-benar kehabisan (semua cooldown atau semua gagal). Circuit-breaker gateway-level tidak disentuh. OFF-path (flag OFF / singleton hardcoded providers) 100% tidak berubah — hanya berlaku ketika `llm.useDynamicProviders` ON dan role punya 2+ provider aktif. **Bonus:** juga memperbaiki bug broken state di `manager.ts` (resolveEffectiveProviders mengembalikan `{primaryList, fallbackList}` tapi generate() mendestructure `{primary, fallback}` → TypeError). Redis-backed cooldown (multi-instance) secara eksplisit **DEFERRED** — owner konfirmasi target realistis 3-5 provider pada single-instance, in-memory `provider-cooldown.ts` sudah cukup. | 3 Sep 2026 | `4cd0371` (N-PROVIDER-ROTATION-UNIT-N1) + `VERIFY-BROKEN-MANAGER-NEVER-HIT-PRODUCTION` (diagnostic-only, confirmed broken state never committed/pushed/deployed — pm2 jalan dari compiled dist konsisten, process start 04:57Z > source broken 11:03Z, 0 TypeError di log production, magic-paste berjalan normal pasca process start) |
| INT-L10 | **skipParams fix** — `openai-compatible.adapter.ts` tambah opsi `skipParams?: string[]` per-provider untuk OMIT parameter tidak didukung backend tertentu. Internal LLM (webai-to-api Gemini proxy) menggunakan `skipParams: ['temperature', 'top_p','response_format']` karena backend menolak ketiganya dengan HTTP 400. `schema.prisma` tambah kolom `skipParams Json?` ke `AIProviderConfig`. `ai-provider-resolver.service.ts` pass-through `skipParams` dari DB row ke adapter. Migration `20260906000001_add_skipparams_to_provider_config` — set `skipParams = ['temperature','top_p','response_format']` untuk Internal LLM row. DB row updated, htpasswd password rotated ke `httpswd` (Basic Auth). Provider lain (Mistral/SambaNova) TIDAK berubah — `skipParams` default `[]` (empty Set). Password di DB ter-enkripsi dengan AES-256-GCM, resolver berhasil decrypt ke `httpswd` (len=7), htpasswd hash diverifikasi via `openssl passwd -apr1 -salt AMZ.ndsN httpswd`. | 6 Sep 2026 | `d4a8be3` (initial: temperature+top_p), `471f50a` (smoke test: +response_format) |
| INT-L10-SMOKE | **Internal LLM smoke test via callV2Engine(jsonMode:true)** — Owner confirmed Gemini browser session re-login (auth status: AUTHENTICATED). Direct curl with `response_format` → HTTP 400 `{"detail":"Unsupported parameter: response_format"}`. WITH `response_format` in skipParams (omitted from body) → HTTP 200, backend returns JSON-wrapped text in markdown code fence. `extractJson()` parses code fence successfully. 2/2 messages validated against V2EngineOutputSchema: (1) "Saya mau beli sepatu warna merah size L" → intent=add_to_cart, confidence=0.9; (2) "Halo, ada diskon nggak?" → intent=product_inquiry, confidence=0.9. pm2 NOT restarted (owner: DO NOT RESTART). RAILS §5: tsc=0, build=0, cooldown 3/3, cascade 3/3. | 6 Sep 2026 | `471f50a` |
| INT-L10-PWD-ROTATION | **Basic Auth password rotation** (was `httpswd`, exposed in chat report) — Generated new random password via `openssl rand`. Updated `/etc/nginx/.htpasswd` via `openssl passwd -apr1 -salt <hex>` (htpasswd CLI not installed — using openssl). nginx reloaded. DB `ai_provider_configs.password` for "Internal LLM" row re-encrypted with new password via `encryptField()` (AES-256-GCM) + raw psql UPDATE (Prisma middleware update path failed at `prisma.ts:91`). `.env` `GOWA_BASIC_AUTH_PASS` updated. Verification: (1) htpasswd hash regen == stored hash ✅, (2) DB pwd len=70 ✅, (3) skipParams preserved `["temperature","top_p","response_format"]` ✅, (4) callV2Engine smoke test SUCCESS (full adapter→DB→decrypt→BasicAuth→nginx→backend chain, 6.5s, intent=add_to_cart confidence=0.9) ✅. Password NOT pasted in any report. pm2 NOT restarted (owner: DO NOT RESTART — resolver cache TTL=30s auto-refreshes). | 7 Sep 2026 | N/A (no code changes committed — htpasswd on server, DB via psql, .env gitignored) |
| Internal-LLM | 3rd-tier fallback "Internal LLM" (Gemini via webai-to-api browser-session proxy, role=chat_fallback_2) — RISIKO: bergantung sesi browser manual (bisa expired kapan saja, perlu re-login manual — bukan API key stabil), TIDAK cocok jadi provider utama jangka panjang. Slot ini DIGANTI oleh konfigurasi provider aktif berbayar/API-key stabil (NVIDIA, GroqNew, SambaNova, B.ai, Gemini, Kilo) — tidak perlu browser-session lagi. Row sudah tidak ada di tabel `ai_provider_configs` (query live 19 Sep 2026: 0 rows). | 19 Sep 2026 | Live query psql 19 Sep 2026 — lihat commit docs-sync ini |
| 35 | **Canary verification gate** — 54 structured test conversations (batch1+batch2) existed in canary store-a3cd7205, but there was no real execution verification of the v2 mapper → cart mutation wire under controlled/manual invocation. This was the gap between having test scenarios on paper and having evidence that the execution path actually works end-to-end against a real DB session. | N/A (verification gate, not a code defer) | Controlled manual execution of the 5 approved scenarios (itu_plus_new_item, partial_cart_cancel, busi_for_two_vehicles, cart_persistence, second_from_top) via the existing `v2-mapper-wire-smoke.ts` script against canary store-a3cd7205, with DB readback before/after. | 9 Sep 2026 | GAP1-FIX (`5c4220a`/`1e84985` — PWA /message idempotency closed), UNIT6-B Unit 1 (`49eb77e` — /handle messageId gap closed), Unit 2 (`1429817` — mapV2ActionsToCartOps), Unit 2a (`ac5dca1` — unresolved propagation), Unit 3 (`fdde243` — controlled wire script), Unit 4 (5/5 scenarios verified). All matched expected behavior including highest-risk partial-cancel-no-wrong-item-removed and NOT_FOUND-not-silently-substituted. **This validates the EXECUTION PATH works correctly under controlled/manual invocation. It does NOT mean any store's engine flag has been flipped to 'active' mode — that remains a separate, not-yet-made decision requiring explicit owner approval per contract §0.** Cleanup: all 5 throwaway scenario conversations + 3 throwaway test products removed post-verification; canary conversation count back to 284 (54 batch1/batch2 + other pre-existing test data, untouched). Cleanup was a pure DB operation, not a commit. |
| 28 | Chat engine architecture overhaul **SELESAI** — v2-rewrite 'active' menjadi jalur utama tunggal untuk semua toko; `interpreter.ts` (v1) + 11-tier keyword tier chain (`fallback.service.ts`) **retired**; intent classification sekarang **LLM-first structured-output** (`V2EngineOutput`) sebagai primary router, keyword tiers hanya emergency fallback. v1/v2 dikonsolidasikan ke satu path. | 14 Sep 2026 | UNIT6-B canary gate closed (9 Sep 2026: `5c4220a`/`1e84985`, `49eb77e`, `1429817`, `ac5dca1`, `fdde243`) + P5 active cutover (14 Sep 2026). Regression gate: golden **35/35**, chat **73/73**, payment **6/6**, cancel/qty/shipping **5/5**, `tsc=0`, `build=0`, DB bersih. |
| 29 | **Status data dummy — RESOLVED**: seluruh toko di sistem adalah data uji/dummy, tidak ada merchant produksi/asli yang terdaftar. Dengan P5 cutover ke 'active' untuk semua toko, syarat canary Fase 2 (30 percakapan customer asli) **tidak berlaku** — full regression suite berfungsi sebagai pengganti. Fakta ini tetap tercatat; merchant asli pertama yang terdaftar akan memicu review ulang canary bila diperlukan. | 14 Sep 2026 | `PROJECT-CONTRACT-CHAT-ENGINE-V2-REWRITE.md` §7 amandemen (14 Sep 2026) — P5 cutover lebih cepat dari rencana karena seluruh data dummy |
